{
  "_comment": [
    "Optional. Copy to build/config.json to enable the few features that need to",
    "know an id on YOUR server. Every value is optional and defaults to empty;",
    "each consumer explicitly guards for that, so leaving this file out is fine.",
    "Find a library id by opening the library in Jellyfin and reading topParentId",
    "(or parentId) out of the URL."
  ],
  "musicLibraryId": "",
  "audiobookLibraryId": "",
  "sportsBridgeUrl": "",
  "drawerHideHrefs": []
}
